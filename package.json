{
  "name": "prime-rise",
  "private": true,
  "description": "Prime Rise marketing site + Vercel API for waitlist signups",
  "scripts": {
    "deploy": "npx vercel deploy --prod",
    "deploy:preview": "npx vercel deploy"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.4"
  }
}
