/**
 * WhatsApp number for static HTML pages (index, FAQ, legal).
 * Use the same digits as Vercel → NEXT_PUBLIC_WHATSAPP_NUMBER.
 * International format, digits only — no + or spaces.
 * Example (US): +1 281-555-1234 → 12815551234
 * Leave empty to hide the floating button on static pages.
 */
window.__PRIMERISE_WHATSAPP__ = "";
